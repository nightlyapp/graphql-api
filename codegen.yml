overwrite: true
schema: "http://localhost:7000/graphql"
documents: null
generates:
  src/graphql:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
    config:
      minify: true
